#include <array>
#include <vector>
#include "piece.hpp"
#include "offsets.hpp"

std::vector<Coordinate> calculationDelegator(Piece* piece, Coordinate position)
{
	switch (piece->getSymbol()) {
	//case('p'): return offsetsPawn(piece, position);
	}
}